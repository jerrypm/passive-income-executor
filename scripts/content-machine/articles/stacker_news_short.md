# Stacker News Daily Discussion — Short Version

**Built 12 passive income streams from the terminal — honest results after a few weeks:**

Set up a Mac Mini with Ollama (local AI), nak CLI (Nostr), and self-hosted LNbits. Built a NIP-90 DVM that sells AI inference on Nostr (kind 5050), an Ollama API with Lightning paywall (10 sats/request via LNbits), listed digital products on Shopstr (NIP-15), and automated Nostr posting via cron.

**Honest earnings:** Bandwidth sharing is the only stream earning real money (~$3-5/mo). Nostr zaps trickle in. Lightning-gated API and DVM earn zero — the ecosystem is too early. Shopstr has near-zero traffic.

**But:** Total cost was $0, everything is self-hosted, no KYC anywhere in the Bitcoin stack, and zero dependencies on platforms that can rug you. The DVM script is ~350 lines of pure Python with BIP-340 Schnorr signing. No npm install, no frameworks.

The bet is that Nostr's machine-to-machine payment layer (DVMs + Lightning) will mature the same way Lightning itself did. Building infrastructure now while it's quiet.

Anyone here running DVMs or Lightning-gated APIs? Curious what's actually generating sats in the wild.

Tools: Ollama, nak, LNbits, Aperture, Shopstr. All open source, all from the terminal.
